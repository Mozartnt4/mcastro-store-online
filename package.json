{
  "name": "mcastro-store-pro-enterprise",
  "version": "4.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check public/app.js && node --check public/enhanced.js && node --check public/online.js && node --check public/service-worker.js",
    "test": "npm run check"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
